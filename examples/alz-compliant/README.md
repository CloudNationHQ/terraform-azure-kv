# ALZ compliant

This example deploys a key vault that satisfies the alz governance policy key vault guardrails.

Secrets and keys are data plane resources, they require a private runner that reaches the
vault over the private endpoint.
